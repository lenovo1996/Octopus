# T02 — Handoff

- Date: 2026-09-22
- Status: done
- Milestone/gate: M0 — PASS (dev app native mở, checks cơ bản pass, đủ 3 inspector states)
- Requirements covered: FR-18 (shell 3 cột), FR-22/23/24 (P2 ẩn/Planned/read-only),
  UI spec §2/§4/§6 (geometry, 3 states, controls), QA rubric T02 (visual 2 sizes)

## Điều hiện đã hoạt động

- Shell 3 cột theo `example.webp`: TopBar (Open/Clone/Init, branch, search, Identity,
  menu, Demo/Native badge) · GitToolbar (Fetch/Pull/Push/Branch/Stash/Pop stash,
  disabled có lý do task; Undo/Redo ẩn theo FR-22) · Sidebar · HistoryPane ·
  Inspector · StatusBar.
- 3 inspector states: Working changes (Unstaged/Staged + diff preview + commit footer),
  Commit details (metadata, parents, diff, Back), Conflict (banner, file, Use
  current/incoming, Mark resolved disabled, Back).
- Sidebar P2 đúng PRD: Pull requests “Planned” + giải thích, Submodules read-only.
- Splitter kéo chuột + phím Left/Right + double-click reset; widths clamp 180–300 /
  320–520. Shortcuts: Ctrl+F search, Ctrl+R refresh preflight, Esc về working.
- Toàn bộ T02 là mock fixture (`src/mocks/demoRepo.ts`) gắn nhãn Demo data ở browser;
  native dùng preflight thật cho status bar. Không nút bấm giả: mọi control chưa nối
  đều disabled có `title` nêu task nối thật.

## Checks và evidence

| Command / procedure | Result | Evidence | Giới hạn |
|---|---|---|---|
| `pnpm check` | passed | 0 errors, 0 warnings | 2 a11y warnings của splitter đã gắn `svelte-ignore` + lý do spec §2 |
| `pnpm lint` | passed | exit 0 | — |
| `pnpm test:unit` | passed | 11/11 (`ipc` 6 + `shell` 5: clamp, state cycle, fixture invariants) | Fixture topology, chưa phải graph layout T05 |
| `cargo test/clippy/fmt --locked` | passed | 8/8, clippy sạch, fmt sạch | Không đụng Rust ở T02 |
| `pnpm build` | passed | `dist/` 57.7 KiB JS | — |
| Native screenshots 3 states @1440×900 | passed | `docs/evidence/T02/t02-{working,commit,conflict}.png` | States chụp bằng default tạm + HMR, đã revert |
| Native screenshot @1280×800 | passed | `docs/evidence/T02/t02-working-1280x800.png` | Ba cột nguyên, không clipping; đổi `tauri.conf` tạm, đã revert 1440×900 + rebuild |

## Quyết định và thay đổi contract

- Không đổi contract/DTO. Lane graph ở HistoryPane là placeholder tĩnh trong fixture;
  T05 thay bằng pure layout + virtual list.
- Commit footer hiện message input + nút disabled (ghi “wires real index in T10”).

## Blockers / known issues

- Không có blocker. Lưu ý: `pkill -f '<pattern>'` tự khớp shell gọi nó khi cmdline
  chứa pattern — dùng `pkill -x <name>` hoặc pattern dạng `vit[e]`.

## Next action

Bắt đầu **T03 — Git runner, repo discovery, open/init/recent** (depends T01 done):
đọc Git engine §1–3 + security, dựng bounded subprocess runner trước.
