# T01 — Handoff

- Date: 2026-09-22
- Status: done
- Milestone/gate: M0 (partial — scaffold + native shell; full M0 gate closes at T02)
- Requirements covered: T01 acceptance (scaffold, scripts, toolchain pin, typed
  error/result, mock/real adapter boundary, preflight command, native IPC smoke)

## Điều hiện đã hoạt động

- Scaffold Tauri 2 + Svelte 5 + TypeScript strict + Vite SPA tại root. Frontend dựng
  tay (không SvelteKit) theo docs/03-architecture.md; chỉ `src-tauri` (config, icons,
  capabilities) lấy từ `create-tauri-app@4.7.4` qua staging dir. Docs/`example.webp`
  giữ nguyên.
- `app_preflight` trả Git status thật: `/usr/bin/git`, `2.43.0 (supported)`,
  `linux / x86_64` — đã thấy trên window native (docs/evidence/T01/native-preflight.png).
- Browser preview dùng mock adapter, gắn nhãn **Demo data**, không gọi native API.
- Scripts chuẩn: `pnpm dev|build|preview|check|lint|test:unit|tauri`;
  `test:ui|test:visual|test:native` báo `not yet implemented` (exit 1), chưa tới task.
- Toolchain pin: Rust 1.98.1 (`rust-toolchain.toml`), Node v24.15.0 (`.nvmrc`),
  pnpm 10.34.5 (`packageManager`), lockfiles `pnpm-lock.yaml` + `src-tauri/Cargo.lock`.
- CI skeleton `.github/workflows/ci.yml` (static/unit lane).

## Checks và evidence

| Command / procedure | Result | Evidence | Giới hạn |
|---|---|---|---|
| `pnpm check` | passed | 0 errors, 0 warnings | — |
| `pnpm lint` | passed | exit 0 | TS/JS + config; `.svelte` do `svelte-check` |
| `pnpm test:unit` | passed | 6/6 `tests/unit/ipc.test.ts` | DTO round-trip, stale/mismatch, 27 codes |
| `cargo fmt --check` | passed | exit 0 | — |
| `cargo clippy --all-targets --locked -- -D warnings` | passed | exit 0 | — |
| `cargo test --locked` | passed | 8/8 (envelope, version parse, real-git smoke, requestId validation) | Smoke dùng system Git, không đụng repo |
| `pnpm build` | passed | `dist/` 33.5 KiB JS | — |
| Native dev launch (debug binary + vite :1420, X :1) | passed | window `GitDock` 1440×900, screenshot preflight thật | Debug build cần dev server (tauri `is_dev`); release binary ở T16 |
| `pnpm tauri dev` một lệnh | failed rồi bỏ | vite chết `beforeDevCommand non-zero` sau khi binary start | Dùng vite + binary riêng thì pass; Tauri CLI wrapper cần xem lại ở T02 |

## Quyết định và thay đổi contract

- Frontend Vite SPA thay vì SvelteKit template (đúng architecture “No SSR/SvelteKit
  server”); `frontendDist` = `../dist`, window 1440×900 / min 1100×700.
- `ErrorCode` giữ tên SCREAMING_SNAKE làm wire format (`#[allow(non_camel_case_types)]`
  có chú thích) để khớp contract camelCase-except-codes.
- Không thay đổi DTO/contract; chỉ hiện thực envelope + preflight của IPC v1.

## Blockers / known issues

- `commands/preflight.rs` (đã sửa): argv `git --version -c …` sai thứ tự → exit 129
  (`GIT_ERROR`). Nguyên nhân: flag global `-c` phải đứng trước subcommand. Fix + thêm
  `preflight_reports_real_git` regression test. Đã verify lại bằng screenshot native.
- `pkill -f 'target/debug/gitdoc[k]'` tự khớp shell gọi nó (cmdline chứa path binary)
  → shell ăn SIGTERM hai lần. Dùng `pkill -x gitdock` từ nay.

## Next action

Chạy `ls docs/evidence/T01/` rồi bắt đầu **T02 — Application shell theo mẫu**
(depends T01 done): topbar/toolbar/sidebar/history/inspector/status bar + 3 states mock.
