# T15 — Handoff (Integration, edge cases và performance gate)

- Date: 2026-09-22
- Status: done trong phạm vi làm được headless (gates + perf + native launch smoke + QA mapping). Full native E2E (tauri-driver/WDIO) và UI frame/memory profiler chưa chạy — ghi `blocked` từng gate dưới, không tính pass.
- Milestone/gate: M4
- Requirements covered: docs/08 §5 performance budgets, §7 release gate (một phần), AT/FX mapping

## Điều hiện đã hoạt động (mới trong T15)

- Full gates xanh cuối vòng: `cargo test` 108/108, clippy `-D warnings`, fmt, `pnpm check/lint`, vitest 49/49, `pnpm build`.
- Benchmark engine-level (§5): xem `docs/evidence/T15/perf.md` — status p95 ~10 ms (target 1 s), history-200 ~43 ms (target 2 s), diff-200KiB p95 4 ms (target 300 ms). Dataset 10k commits/5k files (`/tmp/perf-repo`, script `/tmp/bench15.sh`).
- Native launch smoke THẬT (không phải demo): `.deb` build release → giải nén ra `/tmp` (không cài vào máy) → chạy dưới Xvfb với HOME tạm → cửa sổ GitDock 1440×900 render màn hình chào + preflight `Git 2.43.0 (supported) · linux/x86_64`. Evidence: `docs/evidence/T15/native-welcome.png`. Dọn process sau khi chụp.
- License inventory: Rust Linux closure 289 crates — toàn permissive (MIT/Apache-2.0/BSD/ISC/Unlicense/Zlib/Unicode-3.0/MPL-2.0 ×4 file-level, CC0); npm 157 pkgs permissive; không có GPL. App license CHƯA chốt (không có LICENSE file/field — quyết định của chủ project).

## QA mapping (trung thực)

| ID | Evidence hiện có | Trạng thái |
|---|---|---|
| FX-01/02/04/05/06/09/10/11/12 | Rust integration suites (commit/branch/stage/diff/history/merge/stash/remote/redaction) | pass (BE-level) |
| FX-03 page boundary | history paging tests (limit/cursor) | pass (BE-level) |
| FX-07 worktrees/external/lock | status/registry tests | pass (BE-level, một phần) |
| FX-08 bare + 2 clones | `execute_remote_round_trips_push_and_fetch` | pass (BE-level, file remote) |
| AT-01 Daily | commit BE tests + demo DOM | partial — chưa chạy native |
| AT-02/03 | stage/status/history tests + DOM | partial — chưa native integration |
| AT-04 | round-trip + DIVERGED policy (pull --ff-only refuse) | partial — chưa multi-clone native |
| AT-05 | 11 merge tests (2 parents, conflict giữ stash, external) | partial — chưa native |
| AT-06 cancel/errors | cancel test + redaction corpus + poll fallback | partial — chưa network-drop native |
| AT-07 external changes | invalidated listener + version guards (code + unit) | partial — chưa race browser |
| AT-08 package/a11y | .deb build + extracted-run smoke + screenshot; shortcuts scope probe | partial — chưa clean-machine install/uninstall, chưa contrast-meter |
| Perf §5 | engine timings pass; frame/memory | blocked — không có harness |
| Native E2E (tauri-driver/WDIO) | `pnpm test:native` vẫn là stub | blocked — cần quyết định route + display |

## Checks và evidence

| Command / procedure | Result |
|---|---|
| `cargo test --locked` 108/108; clippy; fmt; `pnpm check/lint/test:unit` 49/49; `pnpm build` | passed |
| `/tmp/bench15.sh` (20 warm × 4) | passed, xem perf.md |
| `pnpm tauri build` → .deb/.rpm/.AppImage + sha256 | passed, xem T16 handoff |
| Native launch smoke (Xvfb + HOME tạm) + screenshot | passed (launch smoke, không phải E2E) |
| Headless DOM T07–T14 (`/tmp/t*.py`) | passed (demo mode) |
| Native visual gate T05–T14 + full E2E + clean install | NOT RUN — chờ user/môi trường |

## Chưa làm (tương lai / cần user)

- Full native E2E route (T01/T15 pin tauri-driver vs WDIO), UI frame + memory profiler, clean-machine install/uninstall, Git-missing flow (không gỡ git trên máy user), contrast-meter.
