# T16 — Handoff (Linux package và handoff)

- Date: 2026-09-22
- Status: done phần làm được headless (reproducible build + artifacts + runbook + release notes draft). Clean-machine install/uninstall và public publishing cần chủ project — ghi rõ dưới.
- Milestone/gate: M4 (release candidate)

## Artifacts (build 2026-09-22, `pnpm tauri build`, exit 0)

- `src-tauri/target/release/bundle/deb/GitDock_0.1.0_amd64.deb` (3 059 300 bytes)
- `src-tauri/target/release/bundle/rpm/GitDock-0.1.0-1.x86_64.rpm` (3 060 259 bytes)
- `src-tauri/target/release/bundle/appimage/GitDock_0.1.0_amd64.AppImage` (79 206 904 bytes)
- SHA-256: xem `docs/evidence/T15/artifact-sha256.txt`
- Package meta: `git-dock 0.1.0 amd64`, Depends `libwebkit2gtk-4.1-0, libgtk-3-0`; nội dung: `/usr/bin/gitdock`, `.desktop`, icons hicolor. Không có test server trong release (handlers toàn production; `test:native` chỉ là stub script).
- Source identifier: worktree CHƯA commit (mọi file mới `??` trong `git status`) — không có commit hash để ghi; snapshot = worktree ngày 2026-09-22. Không bịa hash.

## Đã verify

- Build từ scratch trên máy này pass; bundle chứa binary + desktop + icons.
- Extracted-run smoke (không cài hệ thống): app mở cửa sổ 1440×900, preflight OK — `docs/evidence/T15/native-welcome.png`.
- Uninstall cleanliness: KHÔNG cài nên không có gì để gỡ trên máy này; .deb chuẩn không xóa user data (chỉ `/usr/*` + icons/desktop).

## Chưa làm — cần chủ project / môi trường sạch

1. Cài/gỡ/chạy trên baseline sạch (VM/container) + Git-missing flow (không đụng git hệ thống của máy dev).
2. App license (chưa có LICENSE; deps toàn permissive, không GPL — inventory trong T15 handoff).
3. Public publishing (quyết định của chủ project).
4. Full native E2E + visual gate người dùng (kế thừa từ T05–T15).

## Runbook (local)

1. Mở repo: Open → chọn thư mục → Trust (network ops cần trust).
2. Làm việc: Working changes → stage → commit (identity từ Git config).
3. Sync: Fetch/Pull/Push nền, Sync log; Merge qua dialog, resolve conflict, Complete.
4. Sự cố settings: xóa `~/.local/share/com.gitdock.app/gitdock-settings.json` (file corrupt tự backup `.json.corrupt`).
5. Không force push, không autostash, không merge unrelated — app từ chối kèm lý do.

## Release notes draft (0.1.0-rc)

GitDock 0.1.0-rc: Git GUI Linux (Tauri 2 + Svelte 5). Working changes, diff viewer, stage/unstage, commit + branches, fetch/pull/push nền, stash, merge --no-ff --no-commit với conflict inspector (text/add-add/modify-delete; binary/symlink/submodule hướng dẫn external), settings font, drafts per-repo, redacted logs. Chưa có: clone UI, remote add/list, stash drop riêng, native E2E đầy đủ. Yêu cầu Git ≥ 2.43.

## E2E route — user chốt Route A

- Có sẵn: `geckodriver` (/snap/bin), `Xvfb` (/usr/bin), node/pnpm. Chưa có: `tauri-driver`, project chưa có dep `wdio`/`selenium`/`tauri-driver`.
- Quyết định: Route A — giữ Xvfb smoke hiện tại + checklist thủ công khi release. Không thêm dependency. Route B (tauri-driver + WebDriver) hoãn vô thời hạn.

## License — user chốt MIT (done)

- Đã thêm `LICENSE-MIT` (Copyright 2026 GitDock) + field `license = "MIT"` vào `src-tauri/Cargo.toml`; rebuild release gồm metadata mới.

## Clean-install — verified trong docker ubuntu:24.04 (done, máy user không đụng)

- `dpkg -i GitDock_0.1.0_amd64.deb` → thiếu deps (đúng kỳ vọng) → `apt-get install -f` → `Status: install ok installed`, version 0.1.0, package `git-dock`.
- Git-missing flow: chưa cài git, app vẫn mở cửa sổ GitDock 1440x900, sống ổn, log chỉ có warning libEGL headless (vô hại) — không crash.
- Có git 2.43.0: mở app headless (Xvfb), ALIVE + window mapped, log không error/panic/fatal.
- `dpkg -r git-dock`: gỡ sạch (`/usr/bin/gitdock` biến mất, package gone), chỉ còn triggers icon-theme chuẩn.
- Container đã xóa sau test; máy user không cài/gỡ gì.
