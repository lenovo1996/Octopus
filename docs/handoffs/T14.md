# T14 — Handoff (Preferences, operation feedback và polish)

- Date: 2026-09-22
- Status: done BE + FE wiring (native visual gate chung với T05–T13 — chờ user confirm bằng mắt trên cửa sổ thật)
- Milestone/gate: M3
- Requirements covered: IPC §4 settings_get/update, settings migration, error recovery display, keyboard scope, redaction corpus

## Điều hiện đã hoạt động

- `settings_get` / `settings_update` (`commands/settings.rs` + `persistence/store.rs`): Settings v1 (`version`, `fontScale`). Update dùng expected version (sai → `STALE_STATE`), patch whitelist `deny_unknown_fields`, range 0.875–1.25 (NaN/inf refuse). Store migrate file T03-era (thiếu `settings`) bằng defaults; corrupt → backup `.json.corrupt` + default; schema tương lai → backup + default.
- Sửa bug thật do corpus bắt được (`git/remote.rs` `redact_url`): password sau `@` thứ hai (`user@pass@host`) trước đây lọt vào host — giờ anchor `@` cuối + username cắt ở `:,@` đầu.
- FE: menu TopBar (Identity popover từ identity IPC thật, Settings…, Help & shortcuts), `SettingsModal` (slider 87.5–125%, preview live qua 3 CSS vars, Save persist native/mock, concurrent writer → reload), `HelpModal` (4 shortcuts + diagnostics preflight + safety notes), drafts commit per-repo + panel widths trong localStorage (restore on open/boot, autosave on edit), shortcuts không bắn khi đang gõ (Esc blur), nút Clone honesty-label.
- Demo gating giữ nguyên (`demo = !isNative()`); không có test-only IPC trong release handler.
- Tests: 7 Rust settings (migrate/corrupt/future/update/version/whitelist) + 2 redaction corpus + `tests/unit/settings.test.ts` 3 tests mock surface.

## Checks và evidence

| Command / procedure | Result | Evidence | Giới hạn |
|---|---|---|---|
| `cargo test --locked` | passed 108/108 | terminal (+9 T14) | fixtures `/tmp` |
| `cargo clippy --locked --all-targets -- -D warnings`, `fmt --check` | passed | exit 0 | — |
| `pnpm check` / `lint` | 0 errors+warnings / sạch | — | — |
| `pnpm test:unit` | 49/49 (12 files, +`settings.test.ts`) | vitest | — |
| `pnpm build` | OK | dist | — |
| Headless WebKit DOM (`/tmp/t14dom.py` + `/tmp/t14keys.py`) | passed | Identity popover, settings save → `--gd-font-size: 14.6px`, drafts key, help 4 shortcuts + diagnostics, Esc-blur, Ctrl+R bị nuốt trong input | demo mode |
| Native settings/store thật + native visual | NOT RUN | chờ user chung gate | — |

## Chưa làm (tương lai, không phải bug)

- Draft isolation cross-repo mới verify logic + key tồn tại (chưa DOM 2-repo vì demo 1 session).
- Settings chỉ có fontScale (đúng "settings v1" tối thiểu; thêm field = thêm patch key + test).
- 3 inspector visual review + native sync/merge/stash thật: chờ user chung gate.
