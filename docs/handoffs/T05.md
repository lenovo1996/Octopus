# T05 — Handoff (logic done; native visual cần user confirm)

- Date: 2026-09-22
- Status: in_review (native visual gate chưa có screenshot qua được)
- Milestone/gate: M1 tiếp tục
- Requirements covered: FR-05/FR-08/FR-21 (phần graph + details + parent selector)

## Điều hiện đã hoạt động (đã chứng minh)

- Pure lane layout (`src/lib/graph/layout.ts`): first-parent kế thừa lane, merge-leg
  fork lane mới, root giải phóng, carry qua page. Unit tests: linear/merge/carry/
  fan-out + virtual window bounds (10k rows chỉ render ~50 nodes).
- `HistoryPane` virtualized (row 32px, overscan, spacer, `aria-setsize/posinset`,
  sentinel load-more), states loading/error/empty, boundary marker `◇`, ref chips.
- Nối thật: open → `repoRefs` + `historyPage(allRefs, 200)` → accumulate + cursor;
  select commit → `commitDetails` (parent mặc định 0 khi non-root — đã sửa backend
  cho đúng contract); merge có dropdown chọn parent; files list status/path/oldPath.
- Mock adapter phân trang demo cho browser preview.
- Backend `commit_details` mặc định parent 0 cho non-root (trước đây đòi explicit
  cả single-parent — sai contract, đã fix + tests vẫn pass).

## Checks và evidence

| Command / procedure | Result | Evidence | Giới hạn |
|---|---|---|---|
| `cargo test --locked` | passed | 28/28 | — |
| `cargo clippy --locked -- -D warnings`, `fmt --check` | passed | exit 0 | — |
| `pnpm check/lint/test:unit/build` | passed | 0 errors; 19 tests; dist ok (2 a11y warnings đã xử lý: splitter/scroll ignore có lý do, `aria-current` thay `aria-selected`) | — |
| Headless WebKit DOM dump | passed | App mount đầy đủ, Welcome demo render, không console error ( drivers/giỏ `wkdom.py` ngoài repo) | Không có Tauri bridge nên chỉ demo mode |
| Native screenshot graph/details | BLOCKED | Mọi capture `import`/xwd ra trắng 771B dù: page serve 200, HMR socket của chính WebKitNetworkProcess của app đang ESTABLISHED, web process sống, window IsViewable | Đã thử: fresh vite --force, rebuild binary, raise/focus, tắt compositing, Mutter D-Bus (AccessDenied), xdotool (no root). Control terminal capture vẫn tốt → pipeline capture sống, riêng window app trắng |

## Phân tích block

- Page đã chứng minh render qua headless DOM; IPC logic chứng minh qua 28 Rust tests
  (fixtures merge/tag/shallow/detached thật). Phần chưa chứng minh: mắt thấy graph +
  details trên window native.
- Giả thiết còn lại: Tauri webview không navigate (devUrl?) hoặc paint path riêng;
  cần kiểm chứng bằng mắt user hoặc remote-debugger. Không đoán thêm, không sửa mù.

## Next action (cần user, dưới 2 phút)

Mở app đang chạy → Open `/tmp/graph-fixture` → nhìn graph merge + click commit merge
→ đổi Parent 1/2 → trả lời “graph ok / details ok” hoặc chụp màn hình gửi lại.
Sau đó làm **T06 navigation/search**.

## Follow-up 2026-09-23

Graph/diff UI đã được cập nhật theo yêu cầu mới; xem [handoff](UI-graph-diff-2026-09-23.md). Diff hiện mở tại main panel có ×/Escape; mô tả preview trong inspector ở trên là trạng thái lịch sử.
